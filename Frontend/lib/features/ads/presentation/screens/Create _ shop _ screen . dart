import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../../../core/fa_strings.dart';

class CreateShopScreen extends StatefulWidget {
  const CreateShopScreen({Key? key}) : super(key: key);

  @override
  _CreateShopScreenState createState() => _CreateShopScreenState();
}

class _CreateShopScreenState extends State<CreateShopScreen> {
  int _selectedPlan = 0; // 0=پایه, 1=حرفه‌ای
  int _selectedTheme = 0;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(FaStrings.createNewShop),
        centerTitle: true,
      ),
      body: ListView(
        padding: EdgeInsets.all(16),
        children: [
          Text(FaStrings.selectPlan, style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
          // TODO: اضافه کردن اجزای انتخاب پلن، قالب و...
          SizedBox(height: 30),
          ElevatedButton(
            onPressed: () {
              // TODO: ایجاد فروشگاه در سرور
            },
            child: Text(FaStrings.createShop),
          ),
        ],
      ),
    );
  }
}
